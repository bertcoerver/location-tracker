{
  "name": "location-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "deck.gl map of location pings committed to this repo",
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 8000",
    "test": "node --test \"test/**/*.test.js\""
  }
}
